/* This must be replaced by reusable Filter classes */

#ifndef DATASTRUCTURES_H
#define DATASTRUCTURES_H

template <typename T>

struct MinMaxAvg {
    T min {0};
    T max {0};
    T avg {0};
};

#endif